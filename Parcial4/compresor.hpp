#pragma once
#include <string>
#include <vector>

// Comprime una lista de archivos y los guarda en una carpeta destino
void comprimir_archivos(const std::vector<std::string> &archivos, const std::string &carpeta_salida);
